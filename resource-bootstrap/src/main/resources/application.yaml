spring:
  application:
    name: resource-service
  graphql:
    http:
      path: /graphql
  mongodb:
    uri: ${SPRING_MONGODB_URI}
    auto-index-creation: true

server:
  port: 8081

argos:
  resource:
    grpc:
      port: ${ARGOS_RESOURCE_GRPC_PORT}
